<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
	Add a logger config to see logging messages.
	- For more detailed logs, set values to "FINEST"
	- For info on logging levels, see: http://www.vogella.com/tutorials/Logging/article.html
 -->
    <bean id="myLoggerConfig" class="com.spring.learn.LoggerConfig" init-method="initLogger">
        <property name="rootLoggerLevel" value="FINE" />
        <property name="printedLoggerLevel" value="FINE"/>
    </bean>

    <!--- load properties file: sport.properties-->
    <context:property-placeholder location="classpath:sport.properties"/>

    <!--- Define your beans -->
    <!--- define the dependency-->
    <bean id="myFortune"
          class="com.spring.learn.CoachFortuneService">
    </bean>

    <bean id="baseball"
          class="com.spring.learn.BaseballCoach">
        <!--- set up constructor injection -->
        <constructor-arg ref="myFortune"/>
    </bean>
    <bean id="track"
          class="com.spring.learn.TrackCoach">
        <!--- set up constructor injection -->
        <constructor-arg ref="myFortune"/>
    </bean>
    <bean id="archery"
          class="com.spring.learn.ArcheryCoach">
        <!--- set up setter injection -->
        <property name="fortuneService" ref="myFortune"/>
        <!--- inject literal values-->
        <property name="emailAddress" value="${foo.email}"/>
        <property name="team" value="${foo.team}"/>
    </bean>
</beans>
